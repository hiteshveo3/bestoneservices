rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // M0 default: deny every client request. M2 introduces narrowly tested rules.
    match /{document=**} { allow read, write: if false; }
  }
}
