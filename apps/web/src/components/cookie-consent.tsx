"use client";
import { useEffect, useState } from "react";
export function CookieConsent() { const [visible, setVisible] = useState(false); useEffect(() => setVisible(!localStorage.getItem("bos-cookie-choice")), []); if (!visible) return null; const choose = (value: "accepted" | "rejected") => { localStorage.setItem("bos-cookie-choice", value); setVisible(false); }; return <aside className="cookie-consent" aria-label="Cookie choices"><strong>Cookie choices</strong><p>Essential cookies keep this site working. Analytics and marketing cookies stay off unless you accept them.</p><div><button className="button button-secondary" onClick={() => choose("rejected")}>Reject optional</button><button className="button" onClick={() => choose("accepted")}>Accept optional</button></div></aside>; }
