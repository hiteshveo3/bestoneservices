name: best_one_services
description: Best One Services customer app.
publish_to: none
environment: { sdk: ">=3.3.0 <4.0.0" }
dependencies:
  flutter: { sdk: flutter }
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
dev_dependencies:
  flutter_test: { sdk: flutter }
  flutter_lints: ^5.0.0
flutter: { uses-material-design: true }
